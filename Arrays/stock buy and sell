class Solution {
public:
    int maxProfit(vector<int>& prices) {
        
        int minni=prices[0];
        int profit=0;
        for(int i=1;i<prices.size();i++){
            profit=max(profit,prices[i]-minni);
            minni=min(minni,prices[i]);
        }
        return profit; 
    }
};
